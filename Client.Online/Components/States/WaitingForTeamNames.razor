@inherits State<WaitingForTeamNames>

<StateContainer>
    @if(this.SetupPlayer is not null)
    {
        <MudContainer Class="mb-5">
            <MudAlert Severity="Severity.Info">
                @string.Format(L["Components.States.WaitingForTeamNames.SetupPlayer"], this.SetupPlayer.Username)
            </MudAlert>
        </MudContainer>
    }

    <MudContainer Class="mb-10">
        <TeamMembers Players="this.Team.Players" />
    </MudContainer>
    
    <MudContainer>
        @if (this.Teams.Count > 0)
        {
            <MudText>
                <strong>@L["Components.States.WaitingForTeamNames.TeamNames"]:</strong>
                @(string.Join(", ", this.Teams.Select(team => team.Name)))
            </MudText>
        }
        else
        {
            <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
        }
    </MudContainer>
</StateContainer>