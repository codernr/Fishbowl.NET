@inherits Screen

<ScreenContainer>
    <MudText>
        @L["Components.States.ConnectionClosed.Description"]
    </MudText>
    <MudContainer>
        <MudButton
            Variant="Variant.Filled"
            Color="Color.Warning"
            Size="Size.Large"
            FullWidth="true"
            @onclick="_ => this.Dispatcher.Dispatch(new ReloadAction())">
            @L["Components.States.ConnectionClosed.Button"]
        </MudButton>
    </MudContainer>
</ScreenContainer>