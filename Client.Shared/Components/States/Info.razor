@inherits State
@inject IStringLocalizer<Resources> L

<StateContainer Title="@this.Title" Class="@this.ContextClass">
    <MudText>@this.Message</MudText>
    @if (this.Loading)
    {
        <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
    }
</StateContainer>