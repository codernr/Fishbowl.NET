@using Microsoft.Extensions.Logging;

@inject ILogger<StateManager> Logger;
@inject IDispatcher Dispatcher;

<MudContainer Class="@($"pa-0 d-flex mud-height-full fade-container {(this.show ? "show" : string.Empty)}")">
@if (this.type is not null)
{
    <DynamicComponent Type="this.type" />
}
</MudContainer>