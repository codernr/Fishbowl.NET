@using Microsoft.Extensions.Logging;

@inject ILogger<StateManager> Logger;

<CascadingValue Value="this">
    @ChildContent

    @if (this.ActiveState is not null)
    {
        @this.ActiveState.Container
    }
</CascadingValue>